// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "node_modules": true // set this to true to hide the "node_modules" folder with the compiled JS files
    },
    "search.exclude": {
        "node_modules": true // set this to true to hide the "node_modules" folder with the compiled JS files
    }
}